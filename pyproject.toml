[project]
name = "simple-pomodoro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=14.0.0",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
